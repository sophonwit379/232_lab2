#include<stdio.h>
int main()
{
    int a,b,c,i,max,min;
    char ch[3];
    scanf("%d %d %d",&a,&b,&c);
    int num[3]={a,b,c};
    if(a>b && a>c)
        max=a;
    else if(b>a && a>c)
        max=b;
    else
        max=c;
    if(a<b && a<c)
        min=a;
    else if(b<a && b<c)
        min=b;
    else
        min=c;
    scanf("%s",ch);
    for(i=0;i<3;i++)
    {

    }





}
